import { RepeatPatternPipe } from './repeat-pattern.pipe';

describe('RepeatPatternPipe', () => {
  it('create an instance', () => {
    const pipe = new RepeatPatternPipe();
    expect(pipe).toBeTruthy();
  });
});
